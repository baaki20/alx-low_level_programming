
#include <stdio.h>

/**
 * _strspn - Gets the length of the initial segment of an s which consists
 * entirely of bytes from a string accept.
 *
 * @s: A pointer to the string to be scanned.
 * @accept: A pointer to the string containing the characters to match.
 *
 * Return: The number of bytes in the initial segment of s which consist of
 * bytes from accept.
 */
unsigned int _strspn(char *s, char *accept)
{
	return (0);
}

